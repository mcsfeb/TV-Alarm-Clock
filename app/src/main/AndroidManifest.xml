<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Schedule alarms at exact times (auto-granted for alarm clock apps) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Needed on some Android 12+ devices that don't auto-grant USE_EXACT_ALARM -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Turn the TV screen on when alarm fires -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Re-register alarms after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Fetch thumbnails and content metadata (future use) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- See which streaming apps are installed (required on Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Keep ContentLaunchService alive during streaming app cold-start -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required on Android 14+ (SDK 34+) when foregroundServiceType="mediaPlayback" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Stop the app before launching to ensure it starts from a clean state -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- This is a TV app -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <!-- TV has no touchscreen -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVAlarmClock">

        <!-- Main screen - appears in the Android TV launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Alarm firing screen - full screen alarm display -->
        <activity
            android:name=".ui.screens.AlarmActivity"
            android:exported="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true" />

        <!-- Fires when the alarm goes off -->
        <receiver
            android:name=".service.AlarmReceiver"
            android:exported="false" />

        <!-- Re-registers alarms after the TV reboots -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Exported test receiver for ADB live testing of ContentLaunchService.
             Trigger via: adb shell am broadcast -a com.mcsfeb.tvalarmclock.TEST_LAUNCH
             with extras: pkg, uri, channelName, searchQuery, vol -->
        <receiver
            android:name=".service.TestLaunchReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.mcsfeb.tvalarmclock.TEST_LAUNCH" />
            </intent-filter>
        </receiver>

        <!-- Foreground service for content launch + profile bypass -->
        <service
            android:name=".service.ContentLaunchService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Accessibility service for auto-clicking past profile screens -->
        <service
            android:name=".service.AlarmAccessibilityService"
            android:exported="false"
            android:label="TV Alarm Clock - Auto Profile Select"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
